#include "main.h"

/**
 * _strlen - a function that returns the lenght of a string
 *
 * @s: input char
 *
 * Return: it returns the lenght in in
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
