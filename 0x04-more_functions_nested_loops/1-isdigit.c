#include "main.h"

/**
 * _isdigit - this function check for digits
 *
 * @c: this takes an integer input
 *
 * Return: 1 if it is a digit 0 if otherwise
 *
 */

int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	{
		return (0);
	}
}
