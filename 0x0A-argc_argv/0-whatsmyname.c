#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: counter
 * @argv: argument input
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv);
	return (0);
}
